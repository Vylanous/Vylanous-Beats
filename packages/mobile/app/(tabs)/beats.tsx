import { useQuery } from "@tanstack/react-query";
import { useMemo, useState } from "react";
import { ActivityIndicator, FlatList, RefreshControl, StyleSheet, Text, TextInput, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import { BeatCard } from "../../components/BeatCard";
import { BrandHeader } from "../../components/BrandHeader";
import { fetchBeats } from "../../lib/api";
export default function BeatsScreen(){const [q,setQ]=useState("");const query=useQuery({queryKey:["beats"],queryFn:fetchBeats});const beats=useMemo(()=> (query.data??[]).filter(b=>`${b.title} ${b.genre} ${b.mood} ${b.tags}`.toLowerCase().includes(q.toLowerCase())),[q,query.data]);return <SafeAreaView style={s.page} edges={["top"]}><View style={s.header}><BrandHeader eyebrow="BEAT CATALOG"/><Text style={s.title}>Find your sound</Text><Text style={s.copy}>Live drops, ready for your next release.</Text><TextInput value={q} onChangeText={setQ} placeholder="Search beats, moods, or genres" placeholderTextColor="#77727F" style={s.search}/></View>{query.isLoading?<ActivityIndicator color="#A855F7" style={{marginTop:40}}/>:<FlatList data={beats} keyExtractor={b=>b.id} renderItem={({item})=><BeatCard beat={item}/>} contentContainerStyle={s.list} ItemSeparatorComponent={()=> <View style={{height:10}}/>} refreshControl={<RefreshControl refreshing={query.isRefetching} onRefresh={()=>query.refetch()} tintColor="#A855F7"/>} ListEmptyComponent={<Text style={s.empty}>No beats match that search.</Text>}/>}</SafeAreaView>};const s=StyleSheet.create({page:{flex:1,backgroundColor:"#0B0A11"},header:{paddingHorizontal:18,paddingTop:18},title:{color:"#F8F6FB",fontSize:30,fontWeight:"900",marginTop:20},copy:{color:"#A9A4B0",marginTop:6,fontSize:13},search:{backgroundColor:"#17141F",borderWidth:1,borderColor:"rgba(255,255,255,.08)",color:"#fff",borderRadius:14,paddingHorizontal:15,paddingVertical:13,marginTop:18,fontSize:14},list:{padding:18,paddingTop:14,paddingBottom:100},empty:{color:"#A9A4B0",textAlign:"center",marginTop:50}});
