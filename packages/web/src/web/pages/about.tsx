/** Vylanous about page: rendered from the Site Builder configuration. */
import { ManagedPage } from "../components/managed-page";

export default function About() {
  return <ManagedPage path="/about" />;
}
