/** Vylanous beat catalog: rendered from the Site Builder while retaining live catalog controls. */
import { ManagedPage } from "../components/managed-page";

export default function BeatsPage() {
  return <ManagedPage path="/beats" />;
}
