/** Vylanous homepage: rendered from the global Site Builder configuration. */
import { ManagedPage } from "../components/managed-page";

export default function Index() {
  return <ManagedPage path="/" />;
}
